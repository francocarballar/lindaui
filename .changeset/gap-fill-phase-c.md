---
"@lindaui/blocks": minor
---

Fase C del reemplazo de `@bienflow/ui-kit`: nuevos blocks `time-range-picker` (presets + rango custom, sin `@internationalized/date`), `entity-health-card` (lista de salud genérica) y `fetching-indicator`; `stat-card` gana `loading` (skeletons de value/delta/description).
