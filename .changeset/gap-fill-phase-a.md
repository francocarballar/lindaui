---
"@lindaui/ui": minor
---

Fase A del reemplazo de `@bienflow/ui-kit`: nuevo `icon-button`; `input` extendido con `startContent`/`endContent`/`description`/`errorMessage`; nuevos hooks `use-debounce`/`use-previous`/`use-theme`; `useIsTablet` en `use-media-query`; nuevas utilidades puras `date`/`format`/`time-grouping` (sin dependencia de `dayjs`).
