"use client";
// v3 typography lives under `Typography` (+ Heading/Paragraph/Code/Prose).
// Exposed as Text for a stable name, with the headings re-exported alongside.
export {
  Typography as Text,
  Heading,
  Paragraph,
  Code,
  type TypographyProps as TextProps,
} from "@heroui/react";
